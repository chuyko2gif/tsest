# 🎯 БЫСТРАЯ ИНСТРУКЦИЯ - DRAG & DROP ДЛЯ ЧЕРНОВИКОВ

## ⚡ ЧТО СДЕЛАНО

✅ **Перетаскивание работает** - сохраняется в базу данных  
✅ **Нет лишней пустоты** - элемент полностью убирается при перетаскивании  
✅ **Порядок сохраняется** - после перезагрузки страницы все на месте

---

## 🚀 КАК ЗАПУСТИТЬ (2 ШАГА)

### ШАГ 1: Выполните SQL в Supabase

1. Откройте **Supabase Dashboard**
2. Перейдите в **SQL Editor**
3. Скопируйте и выполните весь код из файла:
   ```
   sql/add_draft_ordering_system.sql
   ```

### ШАГ 2: Перезапустите приложение

```bash
npm run dev
```

---

## ✨ ГОТОВО!

Теперь в разделе **"Архив (Черновики)"**:

- 🖱️ Можно перетаскивать черновики мышью
- 💾 Порядок автоматически сохраняется в БД
- 🚫 Нет пустоты при перетаскивании
- 🔄 Порядок сохраняется после перезагрузки

---

## 🧪 ТЕСТИРОВАНИЕ

1. Откройте кабинет → "Архив (Черновики)"
2. Если черновиков нет - создайте 2-3 штуки
3. Перетащите черновик на новое место
4. Обновите страницу (F5)
5. ✅ Порядок должен сохраниться!

---

## 📁 ИЗМЕНЕННЫЕ ФАЙЛЫ

- `sql/add_draft_ordering_system.sql` - ✨ НОВЫЙ
- `app/cabinet/components/UserReleases.tsx` - изменен
- `app/cabinet/components/releases/ReleasesGrid.tsx` - изменен  
- `app/cabinet/components/releases/ReleaseCard.tsx` - изменен
- `app/cabinet/components/releases/hooks.ts` - изменен

---

## 💡 КАК ЭТО РАБОТАЕТ

**БЕЗ БАЗЫ ДАННЫХ НЕ ЗАРАБОТАЕТ!**

1. Выполните SQL скрипт (добавляет поле `draft_order`)
2. Перетащите черновик → вызывается функция `reorder_draft_release()`
3. База данных пересчитывает позиции → убирает пропуски
4. Приложение загружает черновики отсортированные по `draft_order`

---

## ❗ ВАЖНО

- Перетаскивать можно **только черновики** (status = 'draft')
- Работает для **обеих таблиц**: `releases_basic` и `releases_exclusive`  
- Если не выполнить SQL → **перетаскивание не будет работать**

---

## 🆘 ЕСЛИ НЕ РАБОТАЕТ

1. **Проверьте SQL:** выполнили ли скрипт полностью?
2. **Проверьте консоль:** есть ли ошибки в браузере? (F12)
3. **Проверьте БД:** есть ли поле `draft_order` в таблицах?

```sql
-- Проверка в Supabase SQL Editor:
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'releases_basic' 
  AND column_name = 'draft_order';
```

Если пусто → SQL не выполнился, выполните заново.

---

**Полная документация:** [DRAFT_DRAG_DROP_FIX.md](./DRAFT_DRAG_DROP_FIX.md)
