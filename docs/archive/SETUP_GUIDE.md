# 🚀 Инструкция по запуску обновлённой системы

## ✅ Что было сделано

### 1. **Объединённая история операций**
- В разделе "Финансы" теперь одна лента истории операций
- Начисления на баланс (💰) - зелёные
- Выводы средств (💸) - красные/жёлтые/синие/зелёные (по статусу)
- Всё сортируется по дате в обратном порядке

### 2. **Real-time уведомления**
- Когда админ одобряет/отклоняет заявку - пользователь мгновенно получает уведомление
- Красивая анимированная модалка с confetti при одобрении 🎉
- Shake анимация при отклонении ❌
- Автообновление баланса при одобрении

### 3. **Админ-панель с сайдбаром**
- Красивый sidebar слева
- 9 разделов: Демо, Релизы, Контракты, Архив, Выплаты, Пользователи, Новости, Тикеты, **Заявки на вывод**
- Бейджи OWNER ♛ и ADMIN ★
- Админ может одобрять/отклонять заявки с комментариями

### 4. **Workflow вывода средств**
1. Пользователь подаёт заявку (мин. 1000₽)
2. Статус: "⏳ В обработке"
3. Админ видит заявку в разделе "Заявки на вывод"
4. Админ одобряет/отклоняет с комментарием
5. Пользователь получает уведомление мгновенно
6. При одобрении баланс автоматически обновляется

## 📋 Шаги для запуска

### Шаг 1: Выполнить SQL миграцию
1. Зайти в Supabase Dashboard → SQL Editor
2. Открыть файл `sql/FULL_SETUP.sql`
3. **ВАЖНО:** Заменить email в строке 31:
   ```sql
   UPDATE profiles SET role = 'owner' WHERE email = 'ВАШ_EMAIL@example.com';
   ```
4. Запустить весь скрипт (Run)
5. Проверить результаты внизу - должно быть три галочки:
   - ✅ Роли обновлены!
   - ✅ Таблица withdrawal_requests создана!
   - ✅ Realtime включен!
   - ✅ Полная настройка завершена!

### Шаг 2: Запустить сервер
```powershell
npm run dev
```

Сервер запустится на **http://localhost:3001** (порт 3000 занят)

### Шаг 3: Проверить функционал

#### 3.1 Проверить роли
1. Войти в аккаунт с owner email
2. Перейти в `/admin`
3. Должен быть badge **OWNER ♛**
4. Должны быть доступны все 9 разделов sidebar

#### 3.2 Проверить вывод средств
1. Зайти в `/cabinet`
2. Вкладка "Финансы"
3. Нажать "Вывести средства"
4. Заполнить форму (мин. 1000₽)
5. Отправить заявку
6. Статус должен быть "⏳ В обработке"

#### 3.3 Проверить админ-панель
1. Зайти в `/admin`
2. Sidebar → "Заявки на вывод"
3. Должна отображаться новая заявка
4. Нажать "Одобрить" или "Отклонить"
5. Написать комментарий (опционально)

#### 3.4 Проверить realtime уведомления
1. Открыть `/cabinet` в одной вкладке
2. Открыть `/admin` в другой вкладке
3. В админ-панели одобрить заявку
4. В кабинете должна мгновенно появиться модалка с confetti 🎉
5. Баланс должен автоматически обновиться

#### 3.5 Проверить историю операций
1. В `/cabinet` → "Финансы"
2. Должна быть одна лента с:
   - 💰 Начисления на баланс (зелёные)
   - 💸 Выводы средств (по статусу)
3. Всё должно быть отсортировано по дате

## 🎨 Анимации

### При одобрении заявки:
- ✅ Зелёная модалка
- 🎉 Confetti эффект (30 разноцветных частиц)
- 🎯 Bounce анимация иконки
- 📊 Slide-in эффект модалки

### При отклонении заявки:
- ❌ Красная модалка
- 🔴 Shake анимация иконки
- 💬 Комментарий админа (если есть)

### При выплате:
- ✓✓ Синяя модалка
- 💰 Иконка денег
- ✓ Отметка "Перечислено на карту"

## 🔧 Структура файлов

### Основные компоненты:
- `app/cabinet/page.tsx` - Кабинет пользователя
- `app/admin/page.tsx` - Админ-панель
- `sql/FULL_SETUP.sql` - Полная миграция БД

### Настроенные таблицы:
- `profiles` - роли: basic, exclusive, admin, owner
- `withdrawal_requests` - заявки на вывод
- `payouts` - начисления на баланс
- Realtime подписки включены для всех таблиц

### Новые анимации (tailwind.config.ts):
- `slideInDown` - появление модалки
- `confetti` - падающие частицы
- `shake` - тряска при ошибке

## 🐛 Возможные проблемы

### Realtime не работает
1. Проверить что выполнен скрипт FULL_SETUP.sql
2. Проверить консоль браузера - должны быть логи:
   ```
   🔔 Realtime событие withdrawal_requests: {...}
   ✅ Статус заявки изменен!
   📡 Статус подписки withdrawals: SUBSCRIBED
   ```

### Роль owner не работает
1. Проверить email в profiles:
   ```sql
   SELECT id, email, role FROM profiles WHERE role = 'owner';
   ```
2. Если пусто - обновить:
   ```sql
   UPDATE profiles SET role = 'owner' WHERE email = 'ВАШ_EMAIL';
   ```

### Баланс не обновляется
1. Проверить в админ-панели что статус = 'approved'
2. Проверить консоль - должен быть запрос на обновление баланса
3. Проверить RLS политики в Supabase

## 📚 Дополнительно

### Логика баланса:
```
Баланс = Сумма всех payouts - Сумма всех approved withdrawal_requests
```

### Статусы заявки:
- **pending** - На рассмотрении (⏳ жёлтый)
- **approved** - Одобрено (✓ синий)
- **rejected** - Отклонено (✕ красный)
- **completed** - Выплачено (✓✓ зелёный)

### Права доступа:
- **basic/exclusive** - Могут создавать заявки, видеть свою историю
- **admin** - Могут одобрять/отклонять заявки
- **owner** - Полный доступ ко всем функциям

## ✨ Готово!
Система полностью настроена и готова к работе 🚀
